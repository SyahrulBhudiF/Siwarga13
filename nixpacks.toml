[phases.start]
cmds = [
    "node /assets/scripts/prestart.mjs /assets/nginx.template.conf /nginx.conf && (php-fpm -y /assets/php-fpm.conf & nginx -c /nginx.conf && php artisan queue:work)"
]

[phases.build]
cmds = [
    "pnpm run build",
    "php artisan route:clear",
    "php artisan optimize"
]